services:
  fam-stream:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8040:3000"
    volumes:
      - fam-stream-data:/app/data
    env_file:
      - .env.local
    restart: unless-stopped

volumes:
  fam-stream-data:
